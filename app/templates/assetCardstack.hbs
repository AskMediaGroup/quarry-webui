<div class="alert">
    Are you sure you want run <strong>{{name}}</strong> on this host?
</div>
<div class="alert alert-error">
    This could be dangerous!
</div>
<form class="form-horizontal">
    <div class="btn-group">
        <button class="btn" type="button-primary" {{action "cancel"}}>
            {{icon name="cancel"}} Cancel</button>
    </div>
</form>
<p>Or, to continue:</p>
{{render "confirmation"}}
{{#if ready}}
    <form class="form-horizontal">
        <div class="btn-group">
            <button class="btn btn-primary" type="submit" {{action "run"}}>
                {{icon name="start"}} Run Script</button>
        </div>
    </form>
{{/if}}