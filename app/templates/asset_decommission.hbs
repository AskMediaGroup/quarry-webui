<div class="alert">
    Are you sure you want to decommission <strong>{{content.FQDN}}</strong>?
</div>
<div class="alert alert-error">
    This is a destructive action!
</div>
<form class="form-horizontal">
    <div class="btn-group">
        <button class="btn" type="button-primary" {{action "cancel"}}>
            {{icon name="cancel"}} Cancel</button>
    </div>
</form>
<p>Or, to continue:</p>
{{render "confirmation"}}
{{#if ready}}
    <form class="form-horizontal">
        <div class="btn-group">
            <button class="btn btn-primary" type="submit" {{action "decommission"}}>
                {{icon name="decommission"}} Decommission</button>
        </div>
    </form>
{{/if}}